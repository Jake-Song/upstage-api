[project]
name = "upstage-feedback"
version = "0.1.0"
description = "CLI for Upstage Solar and document APIs"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.34.2",
]
